Prerequisites
-------------
Install

- brew (http://brew.sh/)
- npm (``brew install node``)

In each repo, do
----------------

- Install dependencies with ``npm install``
- Run the tests with ``./tests``
